{
  "name": "hugo-theme-focus",
  "version": "0.0.1",
  "private": true,
  "description": "Hugo theme: Focus, build tools (bootstrap, sass)",
  "scripts": {
    "build": "node-sass-chokidar --source-map true ./src -o ./static/css/",
    "watch": "chokidar 'src/**/*.scss' -c 'npm run build'"
  },
  "author": "lsenta",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "bootstrap": "^4.1.2",
    "chokidar": "^2.0.4",
    "chokidar-cli": "^1.2.0",
    "node-sass-chokidar": "^1.3.0"
  }
}
